# Notebook workflows

The current notebooks cover shared preparation and two baselines. They do not yet implement the proposed four-branch gated model.

| Directory | Runtime | Persistence |
|---|---|---|
| `colab/` | Google Colab | Google Drive |
| `kaggle/` | Kaggle | Saved notebook outputs |

Use only one runtime for a complete experiment. Notebook code is generated by the corresponding script under `scripts/`.
